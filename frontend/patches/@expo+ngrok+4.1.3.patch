diff --git a/node_modules/@expo/ngrok/index.js b/node_modules/@expo/ngrok/index.js
index c9e5f8d..3fd7c5d 100644
--- a/node_modules/@expo/ngrok/index.js
+++ b/node_modules/@expo/ngrok/index.js
@@ -20,6 +20,8 @@ async function connect(opts) {
   }
 
   processUrl = await getProcess(opts);
+  // Laisser le web service ngrok se lier à 127.0.0.1:4040 (sinon ECONNREFUSED immédiat).
+  await new Promise((r) => setTimeout(r, 500));
   ngrokClient = new NgrokClient(processUrl);
   return connectRetry(opts);
 }
diff --git a/node_modules/@expo/ngrok/src/client.js b/node_modules/@expo/ngrok/src/client.js
index 3f748a3..c9a74e2 100644
--- a/node_modules/@expo/ngrok/src/client.js
+++ b/node_modules/@expo/ngrok/src/client.js
@@ -27,19 +27,27 @@ class NgrokClient {
         return await this.internalApi[method](path, { json: options }).json();
       }
     } catch (error) {
+      const res = error.response;
+      if (!res) {
+        throw new NgrokClientError(
+          error.message || "Ngrok request failed (no HTTP response)",
+          undefined,
+          undefined
+        );
+      }
       let clientError;
       try {
-        const response = JSON.parse(error.response.body);
+        const response = JSON.parse(res.body);
         clientError = new NgrokClientError(
           response.msg,
-          error.response,
+          res,
           response
         );
       } catch (e) {
         clientError = new NgrokClientError(
-          error.response.body,
-          error.response,
-          error.response.body
+          res.body,
+          res,
+          res.body
         );
       }
       throw clientError;
@@ -52,8 +60,16 @@ class NgrokClient {
         (response) => response.statusCode === 204
       );
     } catch (error) {
-      const response = JSON.parse(error.response.body);
-      throw new NgrokClientError(response.msg, error.response, response);
+      const res = error.response;
+      if (!res) {
+        throw new NgrokClientError(
+          error.message || "Ngrok request failed (no HTTP response)",
+          undefined,
+          undefined
+        );
+      }
+      const response = JSON.parse(res.body);
+      throw new NgrokClientError(response.msg, res, response);
     }
   }
 
diff --git a/node_modules/@expo/ngrok/src/utils.js b/node_modules/@expo/ngrok/src/utils.js
index 1c2288a..1fe5392 100644
--- a/node_modules/@expo/ngrok/src/utils.js
+++ b/node_modules/@expo/ngrok/src/utils.js
@@ -34,6 +34,13 @@ function validate(opts) {
 
 function isRetriable(err) {
   if (!err.response) {
+    const msg = err && (err.message || err.code || String(err));
+    if (
+      typeof msg === "string" &&
+      /ECONNREFUSED|ETIMEDOUT|ECONNRESET|socket hang up|ENOTFOUND/i.test(msg)
+    ) {
+      return true;
+    }
     return false;
   }
   const statusCode = err.response.statusCode;
